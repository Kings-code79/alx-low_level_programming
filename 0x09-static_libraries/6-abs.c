#include "main.h"
/**
 * _abs - it shows the absolute value of an integer
 * @n: integer in question
 *
 * Return: The aqbsolute value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
